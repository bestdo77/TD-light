# TDlight Python Dependencies
# Install: pip install -r requirements.txt

# Core dependencies
numpy>=1.20.0
pandas>=1.3.0
joblib>=1.0.0

# TDengine Python connector (pip install 'taospy', import as 'taos')
taospy>=2.7.0

# Astropy pinned for feets compatibility (lombscargle location)
astropy<6.0

# Feature extraction for light curves
feets>=0.4

# Machine learning (for classification models)
lightgbm>=3.0.0
scikit-learn>=1.0.0

# ONNX Runtime inference acceleration (~3.7x faster than sklearn)
onnxruntime>=1.14.0

# ONNX export for retraining
onnxmltools>=1.12.0
onnxconverter-common>=1.14.0

# Training HTTP server
flask>=2.0.0
flask-cors>=4.0.0

# Optional: for visualization (uncomment if needed)
# matplotlib>=3.5.0
